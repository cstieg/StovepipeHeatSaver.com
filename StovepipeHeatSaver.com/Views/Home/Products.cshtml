@model IEnumerable<StovepipeHeatSaver.Models.Product>

@{
    ViewBag.Title = "Products";
}

<h2>Products</h2>

<div>
    @Html.Partial("_FindByCircumferencePartial")
</div>

<div class="container">
    <div class="row">
        @foreach (var item in Model)
        {
            @Html.Partial("_ProductPartial", item)
        }
    </div>
</div>